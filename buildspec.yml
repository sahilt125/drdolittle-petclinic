version: 0.1

phases:
  build:
    commands:
      - echo Build started on `date`
      - cd service
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
artifacts:
  files:
    - service/target/drdolittle-petclinic-1.5.2.war
    - appspec.yml
    - codedeploy/*
discard-paths: yes
